a {
  color: var(--text-secondary);
}

p {
  @apply font-serif leading-6 text-lg mb-3;
}

h2 {
  color: var(--text);
  @apply block font-serif text-4xl leading-10 mb-3 mt-10;
}

.subtitle {
  color: var(--text);
  @apply block font-serif text-3xl pb-4;
}
