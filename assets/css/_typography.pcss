a {
  color: var(--text-secondary);
  @apply font-bold transition-colors duration-200 ease-in-out;
}

.title {
  color: var(--color-primary-100);
  @apply block font-bold text-5xl transition-colors duration-200 ease-in-out;
}

.subtitle {
  color: var(--text-secondary);
  word-spacing: 0.15em;
  @apply block font-light text-3xl pb-4 transition-colors duration-200 ease-in-out;
}

.light-mode {
  .title {
    color: var(--color-primary-900);
    @apply block font-bold text-5xl transition-colors duration-200 ease-in-out;
  }
}
